%h1 Regist New Task
= form_for @task do |f|
    .form-group
        = f.label :content, "task content"
        = f.text_field :content, {class:"form-control"}
    // collection_check_boxex [controllerに渡すときのattribute名], [表示する要素], [チェックボックスのid属性に設定するもの], [チェックボックスのname属性に設定するもの]
    // b.check_box ← チェックボクス(input要素)
    // b.label ← チェックボックスの文字(label要素)
    = f.collection_check_boxes :tag_ids, Tag.all, :id, :name do |b|
        = b.check_box class:"checkbox-style"
        = b.label class:"label-style mr-3"
    .form-group
        = f.submit "Regist", {class:"btn btn-primary"}
    = link_to "back to prev page", root_path, {class:"btn btn-default"}   
    